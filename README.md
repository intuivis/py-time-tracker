# py-time-tracker 

This is a Python-based CLI time tracking application.

## Setup

Inside the "data" directory rename the following files:

- example_tasks.json  to tasks.json
- example_time_entries.json to time_entries.json

## Starting the Application

In terminal type "python time-tracker.py"
